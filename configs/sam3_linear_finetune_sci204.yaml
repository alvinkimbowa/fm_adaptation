data:
  raw_data_dir: /home/ultrai/GAA/spinal_cord_injury/data/nnUNet_raw
  results_dir: models
  train_dataset: Dataset204_lesion_czi_B
  test_datasets:
    - Dataset204_lesion_czi_B
    - Dataset206_lesion_120_czi_B
    - Dataset207_lesion_katie_contusion_gfap
  test_split: Tr
  fold: 0

model:
  name: sam3
  probe: linear
  run_name: linear_finetune
  checkpoint: null

training:
  epochs: 20
  min_epochs: 5
  early_stopping_patience: 5
  early_stopping_min_delta: 0.001
  batch_size: 1
  encoder_batch_size: 1
  num_workers: 4
  learning_rate: 0.0001
  weight_decay: 0.01
  seed: 0
  device: cuda

finetuning:
  initial_probe: linear
  initial_probe_checkpoint: final
  encoder_learning_rate: 0.00001
  accumulation_steps: 8
